# Progress Ledger

Purpose: one compact place to track actual project progress so AI agents do not confuse planned work with completed work.

## Status legend
- `not_started`
- `in_progress`
- `blocked`
- `done`

## Checkpoint table
| Checkpoint | Title | Status | Code reality | Last update | Notes |
|---|---|---|---|---|---|
| CHK-000 | Context seed / guardrails / outline | done | baseline context established | 2026-03-21 | keep stable |
| CHK-101 | D1 schema & migrations v1 | done | canonical schema in docs + dedicated migration file in `db/migrations/0001_schema_v1.sql` | 2026-03-21 | migration and seed flow separated |
| CHK-102 | Demo seed data | done | `db/seeds.sql` is insert-only with deterministic timestamps; execution validated | 2026-03-21 | sqlite validation: tenants=1, tours=1, destinations=3, tasks=1 |
| CHK-103 | recomputeSchedule | done | schedule helper in `src/services/schedule.js` + 6 test cases | 2026-03-21 | helper computes arrival/departure from tour.start_date + destination.nights |
| CHK-201 | CRUD tour/destination/service items | done | tours/destinations + operational service-item CRUD + 27 tests | 2026-03-21 | service items now carry assignment, address, stage, channels, and auto-filled scheduling defaults |
| CHK-204 | Multi-tenant guardrails | done | tenant scoped on all queries | 2026-03-21 | X-Tenant-ID header + isolation verified |
| CHK-202 | booking -> tasks + cron/queue reminders | done | generateTasksForTour service + 4 endpoints + 14 tests | 2026-03-21 | tasks from all 5 service types; due notice tracking |
| CHK-203 | threads: email outbound + note inbound | done | thread/message services + 4 endpoints + 15 tests | 2026-03-21 | outbound email logging, internal notes, thread/message retrieval |
| CHK-205 | API contract hardening | done | shared JSON/error helpers + query validation + 7 contract tests | 2026-03-21 | normalized error envelope and ok=true success responses |
| CHK-206 | Worker observability baseline | done | request-scoped logs + trace headers + 4 observability tests | 2026-03-21 | X-Request-ID and Server-Timing on all responses |
| CHK-207 | Day-1 pickup + welcome flow | done | tour-create toggles + auto Day-1 tasks + tour task filter integration | 2026-03-21 | pickup/welcome tasks generated by default; day1 events included in /api/tasks?tourId |
| CHK-208 | Task calendar sync + reminder cadence | done | tenant calendar config + ICS feed + Google preview + cadence endpoints | 2026-03-21 | monthly + 14d/7d/3d reminders for unconfirmed tasks before tour starts |
| CHK-209 | Supplier system baseline | done | supplier CRUD endpoints + service-item supplier_id linkage | 2026-03-21 | generic supplier model without inventory |
| CHK-301 | 3 main tabs + destination sub-tabs | done | static 3-tab ops shell in public asset | 2026-03-21 | Tour Config / Pricing / Visual with basic client-side flows |
| CHK-302 | mini-card service groups | done | destination mini-cards in static shell | 2026-03-21 | all 5 service groups rendered with local add-item flows |
| CHK-303 | itinerary preview md/html | done | itinerary preview route with markdown/html output | 2026-03-21 | destination text toggle + language fallback via query params |
| CHK-304 | Mobile ops surface | done | mobile-task API + note/status actions + shell tab wiring with thread preview | 2026-03-21 | operations-first scope without full builder replication |
| CHK-401 | Domain onboarding flow | done | tenant domain claim + verification status endpoints implemented | 2026-03-22 | `GET/POST /api/domain/config` + `POST /api/domain/verify` |
| CHK-402 | Publish gate | done | tenant publish checklist + publish enforcement implemented | 2026-03-22 | `GET/POST /api/publish/gate` + `on_sale` blocked until checklist passes |
| CHK-403 | Tenancy and billing restrictions | done | tenant billing state + unpaid/trial-expired restrictions implemented | 2026-03-22 | `GET/POST /api/billing/status` + `booked` blocked when billing disallows bookings |
| CHK-404 | Site studio MVP scope | done | site config + multilingual public content + public endpoints implemented | 2026-03-22 | `api/site/*` + `public/site` + `public/tours` with language fallback |
| CHK-405 | Growth & SEO module | done | growth config + seo metadata/slugs + sitemap/robots + lead/event capture implemented | 2026-03-22 | tenant-level distribution layer with multilingual SEO metadata and public hooks |

## Change log
### 2026-03-22 (ui-ops refinement / testing hardening)
- CHK-301..CHK-304 refinement pass completed for test-phase operations flow:
  - compacted top admin/session area into a dense operations header
  - added stage-oriented links for `test`, `preview`, and `live` admin/traveler paths
  - destination UX improvements in Tour Config:
    - compact service toggles
    - drag-handle-only reordering
    - per-destination edit/delete actions
    - bottom-positioned add-destination composer
  - itinerary preview labels now support day ranges by nights (e.g. `Day 7-8`)
- Pricing model evolved from simple band to structured tier configuration:
  - season + season window + pax band + class title + adult shared/single + child shared
  - per-tier delete in both pricing editor and right-side pricing preview
  - save/load wiring through booking settings payload (`pricing_tiers`)
- Global agent-admin controls consolidated:
  - language
  - traveler currency mode (`auto`/`USD`/`VND`)
  - tier input currency (`USD`/`VND`)
  - USD↔VND conversion rate
  - persisted via booking settings API
- Traveler-side runtime behavior improved:
  - auto locale detection (EN/VI baseline text mapping)
  - auto currency display by global mode + locale/location heuristic
  - quote conversion between base currency and display currency
- Backend/settings schema updates:
  - migration `0017_booking_pricing_tiers.sql`
  - migration `0018_booking_currency_locale.sql`
  - booking settings route supports `pricing_tiers`, currency mode/base, and FX rate

### 2026-03-22 (ui-ops follow-up / studio + pricing hardening)
- Admin shell now includes a local Studio layer:
  - fixed `Studio` tab in `public/index.html`
  - rename/reorder/hide/restore for tabs and major cards
  - browser-persisted layout state for local operator customization
- Tenant-level admin date display setting added:
  - `date_time_format` added to booking settings
  - migration `0019_booking_datetime_format.sql`
  - admin booking/mobile surfaces now render `dd.mm.yyyy hh:mm`
- Pricing and currency handling hardened in admin + traveler UI:
  - wider preview panel with richer pricing details
  - pricing composer values now convert when input/base currency switches between `USD` and `VND`
  - sample ladder now loads in the selected input currency rather than fixed VND values
  - traveler checkout/payment labels now follow the active display currency
- Pricing tier/preset wiring corrected:
  - season start/end use date pickers in admin UI
  - pricing `Title` selector now pulls directly from edited class presets
  - tiers now persist and resolve `class_id` for correct traveler quote matching
  - default starter class renamed to `class_standard` / `Standard`
- Full suite remains green at 129/129 tests

### 2026-03-22 (builder persistence via site config)
- Builder workspace persistence added on top of CHK-404 site studio backend:
  - `tenant_site_configs` now stores:
    - `builder_template`
    - `builder_blocks_json`
    - `builder_utilities_json`
  - migration `0020_site_builder_config.sql` added and applied remotely
  - `/api/site/config` now returns and accepts persisted Builder state
  - Builder tab in `public/index.html` now loads from and saves to tenant site config
- Full suite remains green at 129/129 tests after persistence wiring

### 2026-03-22 (builder public payloads + content models)
- Builder now covers all three next-step follow-ups:
  - explicit public builder exposure
  - drag-drop ordering for active blocks/utilities in admin Builder
  - editable persisted content models for hero, FAQ, testimonials, and final CTA
- backend/site config updates:
  - added `builder_content_json` via migration `0021_site_builder_content.sql`
  - `/api/site/config` now accepts and returns `builder_content`
  - `/public/site` now returns top-level `builder`
  - `/public/tours/:id` now returns top-level `builder`
- admin Builder updates in `public/index.html`:
  - active page flow ordering UI
  - active utility ordering UI
  - content editors for hero / FAQ / testimonials / CTA
  - Builder preview now reflects edited content rather than only generic block labels
- Full suite remains green at 130/130 tests

### 2026-03-22 (hosted layout foundation / GrapesJS-ready pivot)
- Strategy direction updated toward external visual editor integration rather than expanding custom Builder complexity
- Implemented hosted layout persistence layer:
  - added `tenant_site_layouts` table via migration `0022_site_layouts.sql`
  - added tenant API endpoints:
    - `GET /api/site/layout`
    - `POST /api/site/layout`
  - stored payload supports `html`, `css`, `project_data`, and `status`
- Implemented Worker-side verified hostname rendering:
  - custom verified domains can now resolve tenant layout directly in Worker
  - placeholder injection foundation added for site/title/contact/tour-list values
- Added focused tests in `test/site-layouts.spec.js`
- Full suite remains green at 133/133 tests

### 2026-03-22 (tenant admin IA restructure)
- Admin shell information architecture was simplified for the GrapesJS pivot:
  - top-level sections are now `Tour Studio`, `Site Studio`, and `Ops`
  - Tour Config and Pricing/Bookings now live under one Tour Studio surface
  - Site Studio is the intended future home for GrapesJS and hosted-layout editing
  - Mobile Ops remains operational, separate from website editing
- Deployed updated `public/index.html` shell structure without changing backend APIs

### 2026-03-22 (grapesjs embed in site studio)
- Embedded GrapesJS directly into Site Studio in `public/index.html`
- Added Site Studio editor controls:
  - load hosted layout
  - seed travel template
  - save draft layout
  - publish layout
- Wired editor to `/api/site/layout` using:
  - `html`
  - `css`
  - `project_data`
  - `status`
- Added a travel starter template containing `{{TOUR_LIST}}` and site/contact placeholders
- Revalidated `test/site-layouts.spec.js`

### 2026-03-22 (site studio cleanup / legacy builder removal)
- Removed the old custom Builder UI from Site Studio
- Kept GrapesJS as the single primary website editor
- Added a dedicated placeholder reference panel for hosted layout tokens
- Repurposed the Site Studio preview panel to reflect hosted layout status instead of legacy block/template selections

### 2026-03-22 (shell layout refinement + sanitization)
- Changed shell behavior so only Tour Studio keeps the split-screen preview
- Site Studio now uses full-width workspace for GrapesJS editing
- Ops no longer uses a split-screen preview layout
- Removed dormant Builder-era client state/functions/preview code from `public/index.html`
- Revalidated `test/site-layouts.spec.js`

### 2026-03-22 (site config compatibility clarification)
- Clarified that legacy `builder_*` fields in `tenant_site_configs` are backward-compatibility fields, not the active Site Studio editing model
- Kept legacy `builder` payloads in public site/tour responses for downstream compatibility
- Added test coverage to ensure modern site-config updates do not require re-sending legacy Builder fields

### 2026-03-22 (chk-405)
- CHK-405: implemented growth & SEO baseline
  - added `src/routes/growth.js`
  - added tenant admin endpoints:
    - `GET /api/growth/config`
    - `POST /api/growth/config`
    - `GET /api/growth/tours/:id/seo?lang=`
    - `POST /api/growth/tours/:id/seo`
    - `POST /api/growth/tours/:id/slug`
  - added public endpoints:
    - `GET /public/tours/slug/:slug?lang=`
    - `GET /sitemap.xml`
    - `GET /robots.txt`
    - `POST /public/leads/contact`
    - `POST /public/events`
  - added migration `db/migrations/0009_growth_seo.sql`
  - added `test/growth.spec.js` with 5 tests

### 2026-03-22 (chk-404)
- CHK-404: implemented site studio MVP backend slice
  - added `src/routes/site-studio.js`
  - added tenant admin endpoints:
    - `GET /api/site/config`
    - `POST /api/site/config`
    - `GET /api/site/pages?page_key=&lang=`
    - `POST /api/site/pages`
    - `GET /api/site/tours/:id/content?lang=`
    - `POST /api/site/tours/:id/content`
  - added public endpoints:
    - `GET /public/site?lang=`
    - `GET /public/tours?lang=&q=`
    - `GET /public/tours/:id?lang=`
  - added migration `db/migrations/0008_site_studio.sql`
  - multilingual fallback behavior implemented for legal pages and tour public content
  - added `test/site-studio.spec.js` with 5 tests

### 2026-03-22 (chk-403)
- CHK-403: implemented tenant billing restrictions baseline
  - added `src/routes/billing.js`
  - added endpoints:
    - `GET /api/billing/status`
    - `POST /api/billing/status`
  - added migration `db/migrations/0007_billing_restrictions.sql`
  - billing state now tracks:
    - `trial_started_at`
    - `trial_ends_at`
    - `subscription_status = trialing | active | unpaid`
  - publish gate now also requires billing in good standing
  - `PATCH /api/tours/:id` with `status=booked` now fails when billing blocks new bookings
  - added `test/billing.spec.js` with 5 tests

### 2026-03-22 (chk-402)
- CHK-402: implemented publish gate baseline
  - added `src/routes/publish.js`
  - added endpoints:
    - `GET /api/publish/gate`
    - `POST /api/publish/gate`
  - added migration `db/migrations/0006_publish_gate.sql`
  - production publish now requires:
    - verified domain
    - payment method added
    - terms accepted
    - commission agreement accepted
  - `PATCH /api/tours/:id` with `status=on_sale` now fails if gate is incomplete
  - added `test/publish-gate.spec.js` with 4 tests

### 2026-03-22 (chk-401)
- CHK-401: implemented tenant domain onboarding baseline
  - added `src/routes/domains.js`
  - added endpoints:
    - `GET /api/domain/config`
    - `POST /api/domain/config`
    - `POST /api/domain/verify`
  - added migration `db/migrations/0005_domain_onboarding.sql`
  - tenant hostnames are unique and tenant-scoped
  - verification states normalized to:
    - `no_domain`
    - `pending`
    - `verified`
  - added `test/domain-onboarding.spec.js` with 4 tests
  - full suite passes with 101/101 tests

### 2026-03-21 (final++++++++++++++)
- CHK-209: implemented supplier system baseline
  - added supplier routes in `src/routes/suppliers.js`
  - added endpoints:
    - `POST /api/suppliers`
    - `GET /api/suppliers`
    - `PATCH /api/suppliers/:id`
  - service-item create/update now supports optional `supplier_id`
  - supplier reference is validated in-tenant when provided
  - added migration `db/migrations/0004_suppliers.sql`
  - added `test/suppliers.spec.js` with 3 tests
  - full suite passes with 97/97 tests

### 2026-03-21 (final+++++++++++)
- CHK-304: implemented mobile ops backend surface
  - added `src/routes/mobile.js`
  - added endpoints:
    - `GET /api/mobile/tasks`
    - `POST /api/mobile/tasks/:id/note`
  - mobile task feed now includes:
    - `thread_id` resolution
    - quick action links (`call`, `sms`, `email`, `whatsapp`, `zalo`) when contact exists
  - quick note endpoint logs note/call-log style entries into related thread by task context
  - added `test/mobile.spec.js` with 3 tests
  - full suite passes with 93/93 tests

### 2026-03-21 (final++++++++++++)
- CHK-304 follow-up: wired static UI shell to mobile ops endpoints
  - `public/index.html` now has a dedicated `Mobile Ops` tab
  - added live fetch flow for `GET /api/mobile/tasks`
  - renders quick contact actions (`call`, `sms`, `email`, `whatsapp`, `zalo`)
  - added inline quick-note composer per task using `POST /api/mobile/tasks/:id/note`
  - full suite remains green with 93/93 tests

### 2026-03-21 (final+++++++++++++)
- CHK-304 completion pass:
  - mobile API now includes status update endpoint:
    - `POST /api/mobile/tasks/:id/status`
  - mobile task feed now returns:
    - `latest_message` thread preview
    - `quick_actions.thread_messages_url`
  - shell Mobile Ops tab now supports:
    - in-card task status update flow
    - latest thread update display
    - thread quick-link action
  - updated `test/mobile.spec.js` coverage to 4 tests
  - full suite passes with 94/94 tests

### 2026-03-21 (final++++++++++)
- CHK-303: implemented itinerary preview endpoint
  - added `GET /api/tours/:id/itinerary`
  - supports `format=markdown|md|html` (default markdown)
  - supports `includeDestinationText=true|false|1|0` (default true)
  - supports `lang` override with fallback behavior
  - output includes merged destination schedule + optional destination text + service summaries
  - added `src/routes/itinerary.js` and router wiring in `src/index.js`
  - added `test/itinerary.spec.js` with 5 tests (markdown/html, text toggle, language fallback, validation)
  - full suite passes with 90/90 tests

### 2026-03-21 (structure refresh)
- Documentation structure/checkpoint refresh completed:
  - normalized AI control docs (`00_AI_INDEX`, `07_REALITY_CHECK`, `20_AI_DISPATCH`) to current runtime reality
  - added CHK-209 (Supplier system baseline)
  - added CHK-304 (Mobile ops surface)
  - aligned roadmap with new strategy docs:
    - `23_SUPPLIER_SYSTEM.md`
    - `24_MOBILE_STRATEGY.md`
    - `25_MOBILE_OPS_RULES.md`

### 2026-03-21 (final+++++++++)
- CHK-208: implemented task calendar sync baseline + tour-start reminder cadence
  - added calendar endpoints:
    - `GET /api/calendar/config`
    - `POST /api/calendar/config`
    - `GET /api/calendar/tours/:id/tasks.ics`
    - `GET /api/calendar/tours/:id/google-sync/preview`
  - added reminder endpoints:
    - `GET /api/tasks/reminders/candidates?at=`
    - `POST /api/tasks/:id/reminders/mark-sent`
  - reminder cadence for remaining unconfirmed tasks:
    - monthly from booking date onward
    - 14d before tour start
    - 7d before tour start
    - 3d before tour start
  - generated tour tasks now include `booking_id` for calendar/reminder linkage
  - added migration `db/migrations/0003_calendar_and_reminders.sql`
  - added `test/calendar.spec.js` with 5 tests for config/feed/sync-preview/cadence/mark-sent
  - full suite passes with 85/85 tests

### 2026-03-21 (direction update)
- Added CHK-208 for calendar/task reminder requirements:
  - sync operational todos to tenant calendars from booking date
  - target calendar channels:
    - Google Calendar
    - iPhone Calendar-compatible feed (ICS/webcal)
  - reminder cadence for remaining unconfirmed tasks:
    - monthly
    - 14 days before tour starts
    - 7 days before tour starts
    - 3 days before tour starts

### 2026-03-21 (final++++++++)
- CHK-207: implemented Day-1 pickup + welcome operational flow
  - extended `POST /api/tours` body with optional toggles:
    - `day1_pickup_enabled` (default `true`)
    - `day1_welcome_enabled` (default `true`)
  - creating a tour now auto-generates first-day tasks:
    - `Day-1 Pickup Coordination`
    - `Day-1 Welcome Setup`
  - generated tasks are linked using:
    - `booking_id = tourId`
    - `service_entity_type = day1_event`
  - `GET /api/tasks?tourId=...` now includes day1-event tasks via `booking_id`
  - updated tests:
    - `test/routes.spec.js` for day1 default/toggle behavior
    - `test/tasks.spec.js` for tour-filter inclusion of day1 events
    - `test/observability.spec.js` setup compatibility update
  - full suite passes with 80/80 tests

### 2026-03-21 (structure update)
- Roadmap structure updated from new product direction:
  - added CHK-207 for Day-1 pickup + welcome operational flow
  - added CHK-405 for tenant-level Growth & SEO / Distribution layer
- Growth framing clarified:
  - not SEO-only
  - SEO + Social + Messaging + Reviews + Analytics + Lead Capture
- Day-1 priority clarified:
  - first day must support pickup + welcome toggles
  - these should become auto-created operational todos with defaults
### 2026-03-21 (final+++++++)
- CHK-201 follow-up: implemented operational service-item CRUD for all 5 destination service groups
  - added generic route layer in `src/routes/service-items.js`
  - added endpoints:
    - `POST /api/destinations/:id/accommodations`
    - `POST /api/destinations/:id/meals`
    - `POST /api/destinations/:id/guides`
    - `POST /api/destinations/:id/local-transports`
    - `POST /api/destinations/:id/intercity-legs`
    - `GET /api/destinations/:id/{group}`
    - `PATCH /api/{group}/{itemId}`
  - service items now support operational fields:
    - `person_in_charge`
    - `contact_name`
    - `contact_email`
    - `address`
    - `stage`
    - `communication_channels`
  - added auto-filled defaults from destination schedule:
    - accommodation check-in / check-out
    - meal datetime
    - guide time window
    - local transport pickup defaults
    - intercity departure defaults
  - service item creation now auto-creates a communication thread per entity
  - added schema extension migration `db/migrations/0002_service_item_ops.sql`
  - added `test/service-items.spec.js` with 9 route tests
  - full suite passes with 77/77 tests
### 2026-03-21 (direction note)
- Product direction clarified before further implementation:
  - public websites must support multiple languages per tenant/tour
  - service mini-cards should turn into operational todos/tasks rather than stay as simple toggles/forms
  - service fields should auto-fill from tour start date + destination order/nights
  - every service operation should capture responsible person, provider contact/email/address, notes, and stage ticks
  - communication channels must include email, Zalo, Messenger, and SMS
  - communication logs should be consolidated directly at the task/service timeline level
- Impacted checkpoints:
  - CHK-302 should be treated as provisional UI shape until todo-first flows exist
  - CHK-303 must consider multilingual itinerary output
  - CHK-404 must include multilingual public-site support
### 2026-03-21 (final++++++)
- CHK-302: extended the static Tour Config shell with destination service mini-cards
  - each destination now renders 5 service-group cards:
    - Accommodation
    - Meals
    - Guide
    - Local Transport
    - Intercity Leg
  - added a per-destination local composer for adding service items into the selected group
  - seeded routes now include representative service examples so the shell shows the intended structure immediately
  - layout remains responsive on mobile and desktop
  - backend suite remains green with 68/68 tests
### 2026-03-21 (final+++++)
- CHK-301: implemented the UI shell MVP in `public/index.html`
  - replaced placeholder static page with an operations shell organized into 3 tabs:
    - Tour Config
    - Pricing
    - Visual
  - Tour Config supports:
    - creating a tour draft through `POST /api/tours`
    - staging destinations locally
    - writing destinations through `POST /api/tours/:id/destinations` after draft creation
  - Pricing tab supports local price-band drafting and sample ladder loading
  - Visual tab supports live theme/headline/copy/accent tuning with a synchronized preview pane
  - added shell status cards for worker health, draft state, and current tour id
  - responsive layout for desktop and mobile
  - backend suite remains green with 68/68 tests
### 2026-03-21 (final++++)
- CHK-206: added worker observability baseline
  - created `src/lib/observability.js` for request context, structured logs, trace headers, and error logging
  - worker now emits structured `request.start`, `request.finish`, and `request.error` logs with request ID, route, status, duration, and tenant context
  - worker responses now include:
    - `X-Request-ID`
    - `Server-Timing`
  - inbound `X-Request-ID` is preserved for cross-system traceability
  - refactored `src/index.js` to dispatch through a request lifecycle wrapper with route names
  - added `test/observability.spec.js` with 4 tests for success, not-found, validation-error, and inbound request ID propagation
  - full suite passes with 68/68 tests
### 2026-03-21 (final+++) 
- CHK-205: hardened runtime API contracts across tours, destinations, tasks, and communication handlers
  - added shared helpers in `src/lib/db.js` for:
    - normalized success responses with `ok: true`
    - normalized error responses with `ok: false` and structured `error.code/message/details`
    - safe JSON body parsing with consistent `invalid_json` / `invalid_body` handling
    - validated pagination parsing for `limit` and `offset`
  - updated route handlers to use shared validation/not-found/internal-error responses
  - added `test/contracts.spec.js` with 7 regression tests for invalid JSON, invalid body shape, normalized 404s, query validation, and semantic validation errors
  - full suite passes with 64/64 tests
### 2026-03-21 (final++)
- CHK-203: implemented communication thread/message handling
  - `getOrCreateThread(entityType, entityId, tenantId, db)` service for entity-scoped threads
  - `addMessage(...)` service for outbound email logs and internal notes
  - `getThreadMessages(...)` and `getEntityThread(...)` query helpers
  - 4 communication endpoints:
    - `POST /api/threads/:entityType/:entityId/email`
    - `POST /api/threads/:threadId/note`
    - `GET /api/threads/:entityType/:entityId`
    - `GET /api/threads/:threadId/messages`
  - tenant-scoped thread/message access enforced on all communication queries
  - 15 communication tests passing
- Router cleanup: rebuilt `src/index.js` dispatch order to remove duplicated/corrupted route blocks and ensure specific thread routes match before generic entity thread routes
### 2026-03-21 (final+)
- CHK-202: implemented task management for touring
  - generateTasksForTour(tourId, db) service: creates one task per service item (accommodation, meal, guide, transport, intercity)
  - 4 task endpoints:
    - POST /api/tasks/generate-for-tour → manual task generation trigger
    - GET  /api/tasks → list tasks with filters (status, tourId)
    - GET  /api/tasks/:id → retrieve single task
    - PATCH /api/tasks/:id → update task (status, due_at)
  - Task generation from 5 service types (accommodations, meals, guides, local_transports, intercity_legs)
  - Due date tracking with getTasksDueForNotice()  - looks ahead 48h by default, filters already-noticed tasks
  - last_notice_at timestamp to prevent duplicate reminders within 24h window
  - All task queries filter by tenant_id (multi-tenant isolation)
  - 14 integration tests: service generation, CRUD operations, filtering, error cases, multi-tenant
- Files: src/services/tasks.js, src/routes/tasks.js, test/tasks.spec.js, updated src/index.js
### 2026-03-21 (final)
- CHK-201: implemented 6 route handlers for tours + destinations CRUD:
  - POST /api/tours → createTour (validation + default lang)
  - GET  /api/tours/:id → getTour (single retrieve)
  - PATCH /api/tours/:id → updateTour (status/title/lang with validation)
  - POST /api/tours/:tourId/destinations → addDestination (+ auto-recompute schedule)
  - PATCH /api/destinations/:destId → updateDestination (+ auto-recompute schedule)
  - GET  /api/tours/:tourId/destinations → listDestinations (ordered by position)
- CHK-201: multi-tenant context via X-Tenant-ID header (defaults to seed tenant)
- CHK-201: all CRUD operations filter by tenant_id for isolation
- CHK-201: error responses normalized (400/404/500 with JSON err messages)
- CHK-201: 18 integration tests covering happy paths, validation, error cases, multi-tenant isolation
- CHK-204: multi-tenant guardrails enforced on all read/write paths
- Files: src/lib/db.js (helpers), src/routes/tours.js, src/routes/destinations.js, test/routes.spec.js, updated src/index.js
### 2026-03-21 (late)
- CHK-103: implemented `recomputeSchedule(tourId, db)` helper function
  - Computes arrival/departure dates for destinations based on tour.start_date + destination.nights
  - Algorithm: first destination arrival = tour start + 2h buffer; each subsequent = previous departure + 4h
  - Departure = arrival + (nights * 24 hours)
- CHK-103: added `persistComputedSchedule(tourId, computedDestinations, db)` to write to D1
- CHK-103: created comprehensive test suite (6 test cases: single dest, multi-dest, error handling, id preservation, DB persist)
### 2026-03-21
- Imported project context export
- Confirmed repo is docs-rich but code-light
- Established AI control docs pack for Copilot/Cline
- Completed one-time full discovery and normalized canonical CHK backlog (CHK-000 -> CHK-404)
- CHK-101: synced canonical CREATE TABLE schema into `docs/DATA_MODEL.sql`
- CHK-101: added dedicated migration file `db/migrations/0001_schema_v1.sql`
- CHK-102: converted `db/seeds.sql` to insert-only deterministic seed data
- Validated schema+seed execution via sqlite3 dry run

## Update template
### YYYY-MM-DD
- Checkpoint: CHK-___
- Status: not_started | in_progress | blocked | done
- Files changed:
  - path/to/file
- Summary:
- Risks / TODO:
- Tests:
